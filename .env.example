# Copie para .env e ajuste. Nunca versione o .env real.
DI_ENVIRONMENT=dev
DI_LOG_LEVEL=INFO

DI_STATE_PATH=data/state.json
DI_OUTPUT_PATH=data/output.jsonl
DI_MANIFEST_DIR=data/manifests

# Fracao de rejeicao acima da qual a carga e bloqueada
DI_MAX_REJECT_RATIO=0.1
